#include <stdio.h>

 int map[256][256];
 int cnt;
void makemap()
{
    int map[256][256];
	register int cnt = 0;
	register int i, k;
    //register int cnt = 0;
	for (i = 0; i < 256;i++)
		for (k = 0; k < 256;k++)
			map[i][k] = cnt++;
}

int main() 
{
	
	for (int i = 0; i < 10000;i++) makemap();


	printf("%d\n", 0);

	return 0;

}
